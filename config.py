"""
config.py — Конфигурация бота.

⚠️  Замените значения на реальные перед запуском!
    Никогда не коммитьте этот файл с настоящим токеном в git.
    Используйте .env / переменные окружения в production.
"""

import os

# ──────────────────────────────────────────────
# Токен бота (получить у @BotFather)
# ──────────────────────────────────────────────
BOT_TOKEN: str = os.getenv("BOT_TOKEN", "8590540170:AAGdc6dQHVnY7sIis9m8FfkkunAzlS_fdok")

# ──────────────────────────────────────────────
# ID основного Канала
# Формат для супергрупп/каналов: отрицательное число, например -1001234567890
# Узнать: переслать любое сообщение из канала боту @getidsbot
# ──────────────────────────────────────────────
CHANNEL_ID: int = int(os.getenv("CHANNEL_ID", "-1003829715647"))

# ──────────────────────────────────────────────
# ID закрытой Группы (с топиками)
# Аналогично — отрицательное число
# ──────────────────────────────────────────────
GROUP_ID: int = int(os.getenv("GROUP_ID", "-1003827311251"))

# ──────────────────────────────────────────────
# Количество приглашённых, необходимых для получения доступа
# ──────────────────────────────────────────────
REQUIRED_INVITES: int = int(os.getenv("REQUIRED_INVITES", "4"))

# ──────────────────────────────────────────────
# Путь к файлу базы данных SQLite
# ──────────────────────────────────────────────
DB_PATH: str = os.getenv("DB_PATH", "referral_bot.db")
